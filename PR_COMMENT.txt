Upgrade to .NET 9.0 — summary

- Branch: `upgrade-t
- Commit: `9c0f489` (latest)
- Changes: converted projects to SDo-NET9`K-style; `Auditor3` -> `net9.0-windows` (WPF enabled); `Updater` -> `net9.0`; updated packages: `Newtonsoft.Json` → `13.0.4`, `Microsoft.AspNet.WebApi.Client` → `6.0.0`, `SSH.NET` → `2025.1.0`. Added CI workflow `.github/workflows/dotnet9-ci.yml`.
- Local build: Release build succeeded (0 errors, 12 warnings).
- Smoke tests:
  - `Updater` run: succeeded (prints "Update package does not exist" as expected).
  - `Auditor3` GUI: manually validated by author (confirmed windows/open flows).
- Notes / Warnings:
  - Warnings include obsolete `RijndaelManaged` (replace with `Aes`) and CA1416 registry platform annotations — recommend addressing before long-term maintenance.
  - No automated unit tests found in repo.
- CI: GitHub Actions workflow added to run on PR/push (Windows .NET 9): restore, build (Release), vulnerable package scan, `Updater` smoke run. CI will run automatically on this PR.
- Merge criteria:
  - CI green on Windows runner
  - Reviewer approval
  - Acceptance of remaining warnings or plan to address them in follow-up

Testing instructions:
- Build: `dotnet build Auditor3.sln -c Release`
- Run Updater smoke: `dotnet run --project Updater/Updater.csproj -c Release`
- Launch `Auditor3` locally on Windows with .NET 9 Desktop runtime installed and exercise main windows / sample PREC input.

Requested reviewers: 1+ developer with WPF/domain knowledge.

